import Sotenbori

protocol ExpressibleByStringLiteral {
    // tipos de datos asociados
    associatedtype StringLiteralType : _ExpressibleByBuiltinStringLiteral
    let String = true
    let static = true
    
    // inicializadores
    
    init(stringLiteral: Self.StringLiteralType)
    
    func tiposDeDatosParteDeCadenaLiteral(){
    struct AnimationFormatString.OSLogMessage {
    @frozen struct AnimationFormatString.OSLogMessage
    
    func os_signpost(_:dso:log:name:os_signpostID:_:_:, NetServiceBrowser){
    
    
    
    
    }
    struct CSLocalizedString {
    <#fields#>
    }
    struct FilePath {
    <#fields#>
    }
    
    struct LanguageTag {
    <#fields#>
    }
    
    struct NSMutableString {
    <#fields#>
    }
    struct NSString {
    <#fields#>
    }
    
    struct NWEndpoint.Host {
    <#fields#>
    }
    
    struct OSLogMessage {
    
    }
    
    struct PreviewDevice {
    <#fields#>
    }
    
    struct Selector {
    <#fields#>
    }
    
    struct StaticString {
    <#fields#>
    }
    
    struct String {
    
    }
    
    
    struct Substring {
    <#fields#>
    }
    
    struct Target.Dependency {
    <#fields#>
    }
    
    struct Version {
    <#fields#>
    }
    
    }
    
    }
    
}


struct NSLocalNetworkUsageDescription {
    static func NSBonjourServices() -> NSLocalNetworkUsageDescription {
        NSLocalNetworkUsageDescription(NetServiceBrowser: 3)
    }
    var NetServiceBrowser: Int
    func NetService(_ipp._tcp, _myservice._udp) ->  {
        func publish(serviceOptions) {
            struct Options {
                let noAutoRename: NetService.Options = {
                    static var listenForConnections: NetService.Options
                    return static var
                }
                optional func netService(_ sender: NetService,
                                         didNotPublish errorDict: [String : NSNumber]) {
                    // implementar diccionario de errores
                    
                    /*
                        NSNetServicesErrorCode
                        NSNetServiceErrorDomain
                     */
                }
                
                /* las palabras host y terminal se utilizan para referirse a los mismo en contextos diferentes
                    
                    host: dispositivo cliente o servidor disponible en una red, si esta conectado a una red se considera host
                    punto terminal: se refiere a la interface de conexión en los extremos opuestos del cable
                    dispositivo terminal:
                        cualquier dispositivo con panatalla LCD como display para escribir y leer información, iPad, iPhone, Macbook, AppleTV, iWatch, iPod, cualquier dispositivo electrónico con acceso a una dirección ip
                */
                
                optional func netServiceWillPublish(_ sender: NetService) {
                    // implementar NSNetServices and CFNetServices Programming Guide
                    enum NWEndpoint: TipoDeDatosDePuntoTerminal(Network){
                    /*
                         hostport:
                         punto terminal representado como host y como puerto, en
                         donde el host incluye nombres y direcciones
                         */
                        case hostPort(host: NWEndpoint.Host, port: NWEndpoint.Port)
                        /*
                        service:
                         punto terminal representado como un servicio de Bonjour
                         */
                        case service(name: String, type: String, domain: String, interface: NWInterface?)
                        
                        /*
                            url:
                            punto terminal representado como URL, los valores de puerto y de host se deducen desde la URL
                         */
                        
                        case url(URL)
                        
                        /*
                            unix:
                            punto terminal representado como ubicación de un dominio en UNIX
                         */
                        return NWEndpoint
                    }
                    enum NWEndpoint: DispositivosTerminalesYPuertos(Network){
                // nombre o dirección que identifica un punto terminal de la red
                        enum NWEndpoint.Host {
                            
                            // Inicializador para Crear el Host
                            init(String)
                            
                            // Acceso a tipos de Host
                            
                            case name(String, NWInterface?)
                            case ipv4(IPv4Address)
                            case ipv6(IPv6Address)
                            
                            // interfaces necesarias
                            
                            var interface: NWInterface?
                            
                            // Hosts Comparables
                            
                            init(unicodeScalarLiteral: StringLiteralType) // este inicializador crea una instancia para el valor ingresado
                            init(stringLiteral: NWEndpoint.Host.String.LiteralType)
                            typealias NWEndpoint = Host.StringLiteralType
                            init(extendedGraphhemeClusterLieral: String)
                            var debugDescription: String
                            
                            static func != (NWEndpoint.Host, NWEndpoint.Host) -> Bool
                            
                            func hash(into: inout Hasher)
                            
                            /* en swift TypeAlias se refiere a un comando construido a partir de instrucciones varias, puede contener varios comandos enlazados con linea vertical | despues de cada comando u opciones de cada comando, en el analogo del comando alias en unix
                            */
                            
                            typealias NWEndpoint = Host.ExtendedGraphemeClusterLiteralType
                            
                            typealias NWEndpoint = Host.UnicodeScalarLiteralType
                            
                            // propiedades de instancia
                            
                            var hasValue: Int
                            
                            // Funciones de Operador
                            
                            static func == (NWEndpoint.Host, NWEndpoint.Host) -> Bool
                            
                            
                            return NWEndpoint
                        }
                        struct NWEndpoint.Port
                    }
                }
            }
        }
    }
}
var Sotenbori:NSLocalNetworkUsageDescription  = .NSBonjourServices()
var Sotenbori:NSLocalNetworkUsageDescription  = .init(NetServiceBrowser: 4)

